{% extends 'catalog.djhtml' %}

{% block content %}
	<h1>{{ c.name }}</h1>
	{{ c.description }}
	<br /><br />
	{% for p in products %}
		{% if p.is_active and p.jars > 0 %}
		{% include 'tags/product_thumbnail.djhtml' %}
		{% endif %}
	{% endfor %}
{% endblock %}
